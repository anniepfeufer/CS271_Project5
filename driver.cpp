#include <iostream>
#include "graph.h"
using namespace std;

int main(void)
{
    vector<int> keys = {1, 2, 3};
    vector<int> data = {1, 2, 3};
    vector<vector<int>> = {{1, 2}}
    Graph(keys, vector<D> data, vector<vector<K>> edges)
}